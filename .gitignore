node_modules/
.env
.env.local

# 티스토리 로그인 세션/쿠키 — 절대 커밋 금지 (계정 유출 위험)
whale-profile/

# 미러링 상태(로컬 전용)
mirrored.json

# 메일 알림 설정 (앱 비밀번호 포함 — 절대 커밋 금지)
.alert-config.json

# 쇼츠 대본용 Anthropic 키 (절대 커밋 금지)
.shorts-config.json
# 파이프라인 실행 로그(로컬 전용)
logs/

# 슬랙 알림 봇 토큰 (절대 커밋 금지)
.slack-config.json

# 워드프레스 계정 (비공개 글 조회용, 절대 커밋 금지)
.wp-config.json

# 로컬 산출물 (쇼츠 대본, 점검 보고서)
쇼츠대본/
reports/
